/**
 * @file HabitTracker.tsx
 * Habit Tracker feature component — stub.
 * Full implementation is TASK-11 in the spec.
 */

/**
 * Displays the user's habits with daily completion checkboxes, streak counts,
 * and a 52-week × 7-day heatmap calendar.
 *
 * @returns The habit tracker UI
 */
export function HabitTracker(): JSX.Element {
  return (
    <div>
      <h2>🔥 Habit Tracker</h2>
      <p>Coming soon — implement in TASK-11.</p>
    </div>
  );
}
