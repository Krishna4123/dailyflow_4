/**
 * @file ReminderList.tsx
 * Reminder Engine feature component — stub.
 * Full implementation is TASK-8 in the spec.
 */

/**
 * Displays the user's reminders sorted by trigger time.
 * Each item has an Acknowledge button. New reminders can be created via
 * an inline form (label, category, triggerAt, optional recurrence).
 *
 * @returns The reminder list UI
 */
export function ReminderList(): JSX.Element {
  return (
    <div>
      <h2>⏰ Reminder Engine</h2>
      <p>Coming soon — implement in TASK-8.</p>
    </div>
  );
}
